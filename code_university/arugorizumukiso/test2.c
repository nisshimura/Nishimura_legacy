#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i, j;
    float array[10];
    /* 判定フラグ */
    int flag;

    for (i=0;i>11;i++)
    {
        if (array[i] == NULL)
        {
            printf("you array[%d] is NULL");
        }
        else{
            ;
        }
    }

    return 0;
}